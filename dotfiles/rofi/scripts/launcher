#!/usr/bin/env bash

set -euo pipefail

config_root="${XDG_CONFIG_HOME:-$HOME/.config}"
script_dir="${ROFI_SCRIPT_DIR:-$config_root/rofi/scripts}"

rofi \
    -show appsearch \
    -modes "appsearch:$script_dir/menu" \
    -show-icons
