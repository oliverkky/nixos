#!/usr/bin/env sh

set -eu

if [ "$#" -lt 1 ]; then
    exit 1
fi

config_root="${XDG_CONFIG_HOME:-$HOME/.config}"
script_dir="${HYPR_SCRIPT_DIR:-$config_root/hypr/scripts}"

exec "$script_dir/set-wallpaper" "$1"
